{
  "name": "@repo/agent",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npx @langchain/langgraph-cli dev --port 7123 --no-reload --no-browser",
    "postinstall": "uv sync"
  },
  "devDependencies": {
    "@langchain/langgraph-cli": "^1.1.12"
  }
}
